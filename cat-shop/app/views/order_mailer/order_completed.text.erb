Une nouvelle commande vient d'être passée
==========================================================

<%=@customer.email%> vient de passer commande.

Le numéro de la commande est <%=@order.id%>, celle ci contient:

<%@order.items.each do |item|%>
<%=item.title%>

<%end%>